/* Welcome to the configuration file! All values are documented with comments next to them. */
/* WARNING: You should only add your non-sensitive project configuration values in this file! */
/* You MUST use the provided .env OR .env.example file to save your tokens and DB links! */
/* This protects your sensitive data from being exposed to the public due to wrong git commits. */

const config = {
  port: 3000, // Configure the application port
  siteAddress: 'https://sifre.org.tr' // Configure the site address
};

export default config;
